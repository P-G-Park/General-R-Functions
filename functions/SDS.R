height_sds = function(height, M_height, L_height, S_height){((height/M_height)^L_height -1) / (L_height * S_height)}
weight_sds = function(weight, M_weight, L_weight, S_weight){((weight/M_weight)^L_weight -1) / (L_weight * S_weight)}
bmi_sds = function(bmi, M_bmi, L_bmi, S_bmi){((bmi/M_bmi)^L_bmi -1) / (L_bmi * S_bmi)}
