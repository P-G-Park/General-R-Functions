require(tidyverse)

# a <- '0.02 - 0.02'
# b <- '0.06 재검중'
# c <- '재검중 0.902'
# d <- '재검중 0.02 재검'
# e <- '.'
# f <- 'QNS'
# g <- '64 재검'

lab_value_tidy <- function(x){
  gsub("^\\.$","",gsub("[^0-9.]","",gsub("\\D+(\\d+\\.\\d+)","\\1",gsub("(\\d+\\.\\d+)\\D+.*","\\1",x))))
}
